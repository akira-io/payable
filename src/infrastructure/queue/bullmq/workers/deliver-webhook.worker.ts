import { PayableError } from '../../../../domain/errors/payable-error';

// TODO: Phase 11
export class DeliverWebhookWorker {
  register(): void {
    throw PayableError.notImplemented('DeliverWebhookWorker (Phase 11)');
  }
}
