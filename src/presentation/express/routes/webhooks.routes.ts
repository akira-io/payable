import { PayableError } from '../../../domain/errors/payable-error';

// TODO: Phase 8
export function registerWebhookRoutes(): unknown {
  throw PayableError.notImplemented('registerWebhookRoutes (Phase 8)');
}
