import { PayableError } from '../../../domain/errors/payable-error';

// TODO: Phase 8
export function registerCheckoutRoutes(): unknown {
  throw PayableError.notImplemented('registerCheckoutRoutes (Phase 8)');
}
