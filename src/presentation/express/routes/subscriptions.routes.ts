import { PayableError } from '../../../domain/errors/payable-error';

// TODO: Phase 8
export function registerSubscriptionRoutes(): unknown {
  throw PayableError.notImplemented('registerSubscriptionRoutes (Phase 8)');
}
